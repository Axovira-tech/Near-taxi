# Near-taxi

This static site is automatically deployed to GitHub Pages on every push to the `main` branch via `.github/workflows/pages.yml`. The workflow packages the HTML, CSS, JS, and assets from the repository root into `_site` before publishing.
